import { OrderNotesPipe } from './order-notes.pipe';

describe('OrderNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
